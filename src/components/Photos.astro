---
import "@/styles/global.css";
const images = ["/photos/1.jpg", "/photos/2.jpg"];
---

<section class="py-3">
  <div class="grid list-none gap-4 p-0">
    {images.map((image) => <img src={image} class="grain" />)}
  </div>
</section>
