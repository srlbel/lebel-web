---
import "@/styles/global.css";
const images = ["/photos/2.jpg"];
---

<section class="py-3">
  <h2 class="text-ink dark:text-dark-ink mb-3 text-lg font-bold">Photos</h2>
  <div class="py-3">
    I'm not a professional, but I enjoy taking photos of landscapes when I go
    out to the countryside.
  </div>

  <div class="grid list-none gap-4 p-0">
    {images.map((image) => <img src={image} class="grain" />)}
  </div>
</section>
